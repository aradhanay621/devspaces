schemaVersion: 2.2.2
metadata:
  name: isolated-mutation-trigger
components:
  - name: tooling
    container:
      image: alpine:latest
      memoryLimit: 512Mi
      command: ['tail', '-f', '/dev/null']
      env:
        - name: VSCODE_DEFAULT_WORKSPACE
          value: "/projects/test-trigger-repo/.code-workspace"
